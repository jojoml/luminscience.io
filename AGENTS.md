# AGENTS.md

Read `CLAUDE.md` in this directory — it is the single source of truth for this repo
(what it is, architecture, conventions, agent rules). This is a pointer file rather
than a symlink because the repo lives inside OneDrive, which breaks symlinks.

Then, before starting work, read the top 2 entries of `docs/DEVLOG.md` and all of
`TODO.md`. End every session by appending a DEVLOG entry, updating TODO.md, and
committing.
