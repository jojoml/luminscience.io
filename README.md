# LuminScience.io

This repository serves the static website for:

```text
https://luminscience.io
```

Local working folder:

```text
/Users/jojo/Library/CloudStorage/OneDrive-Personal/Lumina/PROJECTS/LuminScience.io
```

GitHub remote:

```text
git@github.com:jojoml/luminscience.io.git
```

## Updating The Site

1. Edit the static website files in this folder.
2. Test locally if needed.
3. Commit and push:

```bash
git status
git add .
git commit -m "Update LuminScience site"
git push origin main
```

GitHub Pages should deploy from the `main` branch root.

## Domain

The repo includes a `CNAME` file containing:

```text
luminscience.io
```

Keep this file at the repository root.
